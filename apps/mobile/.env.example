# Leave empty for local dev — API URL is auto-detected from Expo debugger host.
# Only set this for tunnel/staging:
# EXPO_PUBLIC_API_URL=https://your-tunnel.trycloudflare.com

# PostHog analytics — leave empty for local dev (disabled when missing)
# EXPO_PUBLIC_POSTHOG_KEY=
